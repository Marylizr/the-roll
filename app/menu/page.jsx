import MenuClient from "./MenuClient";

export const metadata = {
  title: "Menu",
  description:
    "Explore The Roll's full menu — signature rolls, sashimi platters, makis, and premium beverages. Freshly crafted with sustainable ingredients.",
};

export default function MenuPage() {
  return <MenuClient />;
}
