import CartClient from "./CartClient";

export const metadata = {
  title: "Cart",
  description: "Review your order at The Roll and proceed to checkout.",
};

export default function CartPage() {
  return <CartClient />;
}
